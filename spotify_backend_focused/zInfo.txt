roll Based authorization -
all users can listen the music
only artist users can upload music

npm i express mongoose jsonwebtoken cookie-parser dotenv


modify npx nodemon server.js
in package.json -> scripts add "dev": "npx nodemon server.js" 
now, npm run dev, will run it  npx nodemon serverr.js

add ( used in production to deploy/ start server) ( added changes do not effect user ) ( when changes completed stop it and run again)
"start":"node server.js"

password hashing -- if database get hacked the hackers will only get hashed password not the original, redeable password
( see mdHashgenerator) ( hashing is an algorithnm , cyptography algorithm)

test --> hashing --> f57ffe8fdd526f24a39b69b0ae053ff0

properties -> 
1. same inut same output evertime
2. hash cannot be converted to plain text after hashing ( for the real security , we store hash when user inputs password it matches with the hash)
( so even when database breatched , leaked or compromised then the attacker only gets the hash , and the hash cannot be converted back to plain text  )

performing hashing -
package ( npm i bcryptjs )


in this project we have role based authenticati

power of middleware----
request ke adnadar jo bhi data aata hai middleware sab read kr sakta hai , 
use modify bhi kar sakte hai

middleware response bhi send kar sakte hai